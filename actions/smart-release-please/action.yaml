name: "Smart Release Please"
description: "Runs Release Please but forces correct RC patch increments for fix-only commits."

inputs:
  token:
    description: "GitHub Token (PAT) for pushing commits and triggering releases."
    required: false
    default: ${{ github.token }}

runs:
  using: "composite"
  steps:
    - name: Checkout
      uses: actions/checkout@v6 
      with:
        fetch-depth: 0
        token: ${{ inputs.token }} # Essential for subsequent workflows to trigger

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'

    - name: Promote RC to Stable (Main Only)
      id: promote
      if: github.ref == 'refs/heads/main'
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.token }}
      run: |
        if [ ! -f .release-please-manifest.json ]; then
          echo "No .release-please-manifest.json found. Skipping."
          exit 0
        fi

        CURRENT_VER=$(jq -r '.["."]' .release-please-manifest.json)
        echo "Current Manifest Version: $CURRENT_VER"

        if [[ "$CURRENT_VER" == *"-"* ]]; then
          STABLE_VER="${CURRENT_VER%%-*}"
          echo "🚨 Pre-release detected on main! Promoting to $STABLE_VER"

          git config user.name "mapcolonies[bot]"
          git config user.email "devops[bot]@mapcolonies.com"

          git commit --allow-empty -m "chore: promote to stable $STABLE_VER" -m "Release-As: $STABLE_VER"
          git push "https://x-access-token:${{ inputs.token }}@github.com/${{ github.repository }}.git" HEAD:main
          
          echo "skip=true" >> $GITHUB_OUTPUT
        else
          echo "skip=false" >> $GITHUB_OUTPUT
        fi

    - name: Calculate Correct RC Version
      id: calc
      if: ${{ github.ref_name == 'next' }}
      shell: bash
      run: python3 "${{ github.action_path }}/rc_align.py"

    - name: Inject Release-As Footer
      if: steps.calc.outputs.next_version != ''
      shell: bash
      run: |
        # Configure Git
        git config user.name "mapcolonies[bot]"
        git config user.email "devops[bot]@mapcolonies.com"

        # Check if footer exists (Loop Protection)
        TARGET_VER="${{ steps.calc.outputs.next_version }}"
        LAST_MSG=$(git log -1 --pretty=%B)
        
        if [[ "$LAST_MSG" == *"Release-As: $TARGET_VER"* ]]; then
          echo "Footer already present. Skipping push."
          exit 0
        fi

        # Commit & Push
        git commit --allow-empty -m "chore: enforce correct rc version" -m "Release-As: $TARGET_VER"
        git push "https://x-access-token:${{ inputs.token }}@github.com/${{ github.repository }}.git" ${{ github.ref_name }}

        echo "SKIP_RELEASE=true" >> $GITHUB_ENV

    - name: Close Stale Release PRs
      # Only run if we are NOT skipping (meaning we are ready to run release-please)
      if: env.SKIP_RELEASE != 'true'
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.token }}
      run: |
        echo "Checking for existing release PRs on branch ${{ github.ref_name }}..."
        
        # Find PRs with the autorelease label targeting our branch
        # We store the PR number
        PR_NUMBER=$(gh pr list \
          --base "${{ github.ref_name }}" \
          --label "autorelease: pending" \
          --json number \
          --jq '.[0].number')

        if [ -n "$PR_NUMBER" ]; then
          echo "Found stale release PR #$PR_NUMBER. Closing it to force a fresh regeneration..."
          gh pr close "$PR_NUMBER" --delete-branch
        else
          echo "No stale release PRs found."
        fi

    - name: Run release-please
      if: env.SKIP_RELEASE != 'true'
      uses: googleapis/release-please-action@v4
      with:
        token: ${{ inputs.token }}
        target-branch: ${{ github.ref_name }}
        config-file: release-please-config.${{ github.ref_name }}.json
